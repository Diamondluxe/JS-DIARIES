let heading = document.getElementById("heading"); //only works for id, not class or tag
console.log(heading);

let para = document.querySelector(".text");  // updated version can select id, class, tag, etc


document.getElementById("demo").innerHTML = "Hello This is a Dua Sheikh."; //changes the content of the element with id "demo"

